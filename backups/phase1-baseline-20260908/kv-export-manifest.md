# KV 匯出清單（SYNC_KV，正式環境）

匯出時間：2026-09-22T07:42:08Z
匯出方式：`wrangler kv key get --remote`（唯讀，未刪除、未修改任何 KV 內容）

## 找到的 key

| key | 位元組數 | SHA-256 |
|---|---|---|
| sync:4741 | 897 | 63fbcb995d053545918b5f9c460334052756c9b2de1851bb1aca10dcef97c278 |

## 結構摘要（僅欄位與筆數，不含實際內容）

- 頂層欄位：`ft`, `ins`, `identity`, `quest`（無 `meals`、`ba`，代表這組資料是在飲食占卜／行為拆解功能上線前建立的）
- `ins`（每日打卡紀錄）：1 筆
- `quest.entries`（QUEST 抽卡紀錄）：1 筆
- `identity`：已完成身份測驗，類型為其中一種既有分類

## 注意事項

原始內容（含使用者的每日打卡、身份測驗結果、QUEST 紀錄等實際個人資料）**未提交到 Git**，
僅保存在本次工作環境的 `backups/phase1-baseline-20260908/kv-export/sync_4741.json`（已加入 .gitignore）。

原因：此 key 內含真實的個人飲食行為與情緒資料，避免將個人資料寫入 Git 歷史（尤其是遠端 repo 若為公開儲存庫時無法輕易撤回）。
若需要把原始內容也一併備份進版本控制，請明確告知，會另外處理（例如加密後存放、或存到不對外公開的位置）。
